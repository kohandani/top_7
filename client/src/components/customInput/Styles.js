import { makeStyles } from "@mui/styles";

export default makeStyles({
    labelRoot: {
        right: "20px",
    },
    shrink: {
        transform: "translate(110px , 0px) scale(0.7)!important",
    },
    
});
